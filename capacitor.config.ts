import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.therhenals.contentful',
  appName: 'contentful-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
